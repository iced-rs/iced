## Custom rendering with wgpu

A demonstration of how to use the layout information to perform custom rendering of a widget.

You can run it with `cargo run`:
```
cargo run --package custom_rendering_wgpu
```
