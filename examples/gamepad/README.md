## Gamepad

An application demonstrating gamepad integration.

The **[`main`]** file contains all the code of the example.

You can run it with `cargo run`:

```
cargo run --package gamepad
```

[`main`]: src/main.rs
